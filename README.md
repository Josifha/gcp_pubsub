For executing Order service:

mkdir order-service
cd order-service
npm init -y
npm install @google-cloud/pubsub

For executing Payment service

mkdir payment-service
cd payment-service
npm init -y
npm install @google-cloud/pubsub mysql
